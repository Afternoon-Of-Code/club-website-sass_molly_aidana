---
layout: layouts/base.njk
---
<!--
File: _includes/layouts/post.njk
Purpose: Layout for individual blog posts. Shows post date, content, tags, and navigation links (next/previous).
Student notes:
- Do not edit this file to change post content — edit files under `content/blog/` instead.
- Styling should be added in `css/student.css` if you want to change post appearance.
-->
{# Only include the syntax highlighter CSS on blog posts, included with the CSS per-page bundle #}
<style>{% include "node_modules/prismjs/themes/prism-okaidia.css" %}</style>
<style>{% include "css/prism-diff.css" %}</style>

<p>Posted on: <time datetime="{{ page.date | htmlDateString }}">{{ page.date | readableDate }}</time></p>

{{ content | safe }}

{%- if collections.posts %}
{%- set nextPost = collections.posts | getNextCollectionItem(page) %}
{%- set previousPost = collections.posts | getPreviousCollectionItem(page) %}

{%- if tags.length > 1 or nextPost or previousPost %}
<hr>
{%- endif %}

{%- if tags.length > 1 %}
<p>Tagged with:</p>
<ul>
  {%- for tag in tags | filterTagList %}
  {%- set tagUrl %}/tags/{{ tag | slugify }}/{% endset %}
  <li><a href="{{ tagUrl | url }}">{{ tag }}</a></li>
  {%- endfor %}
</ul>
{%- endif %}

{%- if nextPost or previousPost %}
<p>More posts:</p>
<ul>
  {%- if nextPost %}<li>Next: <a href="{{ nextPost.url | url }}">{{ nextPost.data.title }}</a></li>{% endif %}
  {%- if previousPost %}<li>Previous: <a href="{{ previousPost.url | url }}">{{ previousPost.data.title }}</a></li>{% endif %}
</ul>
{%- endif %}

{%- endif %}
